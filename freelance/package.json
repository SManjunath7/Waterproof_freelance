{
  "name": "mwp-monorepo",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && mv client/dist ./dist",
    "install-all": "cd client && npm install && cd ../api && npm install"
  }
}
