{
  "name": "mwp-monorepo",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build && rm -rf dist && mv client/dist ./dist",
    "install-all": "cd client && npm install && cd ../api && npm install"
  }
}
