{
  "name": "mwp-monorepo",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client",
    "install-all": "npm install --prefix client && npm install --prefix api"
  }
}
